module.exports.items = [ 'item1', 'item2']
const person = {
    name:'bob',
}

module.exports.singlePerson = person
// both items(array) and person(obejct) are expoted and can bes used anywhere along with there properties. 